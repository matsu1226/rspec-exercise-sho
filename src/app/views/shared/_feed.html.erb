<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
    <%# @feed_items が間接的にmicropostsを指していることを検知し、"_micropost.html.erb"をレンダリング %>
  </ol>
  <%= will_paginate @feed_items, params: { controller: :static_pages, action: :home } %>
<% end %>